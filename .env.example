# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Storage
BLOB_READ_WRITE_TOKEN=

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# PET AI does not require paid AI APIs.
# Local audio feature extraction and deterministic interpretation run without provider keys.
# Open-source local vision/audio models can be added later without introducing per-request API billing.
