# Analysis Endpoint

`POST /api/analyze` accepts a species, optional context, and language (`en` or `hi`).

The current implementation is a deterministic MVP placeholder. Replace its inference section with provider-backed acoustic/model inference only after audio ingestion and evaluation infrastructure are ready.