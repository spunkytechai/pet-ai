'use client'
import {useState} from 'react'

export default function AnalyzePage(){const [result,setResult]=useState<any>(null);const [busy,setBusy]=useState(false);async function analyze(){setBusy(true);const r=await fetch('/api/analyze',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({species:'dog',context:'Owner initiated a short vocalization test'})});setResult(await r.json());setBusy(false)}return <main><div className="container"><header className="nav"><div className="brand">PET AI</div><div className="badge">Step 2 · Analyze</div></header><section className="hero"><div className="eyebrow">Sound analysis</div><h1>What might this sound mean?</h1><p>For this MVP, the analysis contract is wired with a safe deterministic fallback. Real audio ingestion and model inference will replace it next.</p><div className="actions"><button className="primary" onClick={analyze} disabled={busy}>{busy?'Analyzing…':'Run sample analysis'}</button></div>{result&&<div className="card" style={{marginTop:28,maxWidth:680}}><div className="eyebrow">Interpretation</div><h3>{result.likely_intent}</h3><p>{result.emotional_state} · {Math.round(result.confidence*100)}% confidence</p><p>Signals: {result.signals.join(', ')||'none detected'}</p><p>Alternatives: {result.alternative_interpretations.join('; ')}</p></div>}</section></div></main>}
