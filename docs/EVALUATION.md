# Evaluation

Track model version, recording ID, predicted intent, confidence, alternatives, context and owner feedback.

Do not use confidence as an accuracy claim until calibrated on held-out data. Split evaluation by animal identity where possible to reduce subject leakage. Report abstention rate, calibration, per-species performance and confusion between common intents.
