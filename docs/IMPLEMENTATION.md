# Implementation

The initial vertical slice includes a landing experience, pet onboarding, sample analysis UI, a schema-validated request contract, deterministic safe placeholder inference, abstention behavior, database tables, environment variables and deployment configuration.

The placeholder inference is intentionally not represented as a trained pet-language model.